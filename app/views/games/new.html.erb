<h1>Add a game</h1>

<%= form_for(@game) do |f| %>

	<%= f.label "Select Game" %>
	<%= f.collection_select :name, @games, :name, :name %><br>

	<h4>or add a new one:</h4>
	<%= f.label :name %>
	<%= f.text_field :name %><br>


	<%= f.submit %>

<% end %>